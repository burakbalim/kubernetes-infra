apiVersion: v1
kind: ConfigMap
metadata:
  name: smtp-config
  labels:
    app.kubernetes.io/component: smtp
data:
  SMTP_MAIL_URL: "{{ .Values.email.smtp.url }}"
  SMTP_MAIL_PORT: {{ .Values.email.smtp.port | quote }}
  SMTP_USERNAME: "{{ .Values.email.smtp.username }}"
  EMAIL_TEMPLATE_DIRECTORY: "{{ .Values.email.emailDirectory }}"
